Planning/
.DS_Store
new_refresh_token.txt

# ML model binaries. NOT regenerated by CI -- no workflow trains or writes
# them; they exist only on the VPS, as whatever was last written there.
BackEnds/the-snake-tank/models/*.joblib
fathom-fall/
__pycache__/

.env
.env.*
!.env.example
