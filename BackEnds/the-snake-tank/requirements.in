# Direct deps only, pinned == to prod fishtank-01's 2026-08-20 pip freeze (captured from /opt/fishtank/BackEnds/the-snake-tank/.venv on the VPS).
# requirements.txt is generated from this — never hand-edit it. Upgrades are deliberate edits here + recompile.
pandas==3.0.1
scikit-learn==1.8.0
lightgbm==4.6.0
boto3==1.42.64
fastapi==0.135.1
uvicorn[standard]==0.41.0
psycopg2-binary==2.9.11
python-dotenv==1.2.2
joblib==1.5.3
